Deno.test(
  "call the log function if the Deno.run() process produces a stdout with length",
  () => {}
);

Deno.test(
  "call the log function if the Deno.run() process produces a stderr with length",
  () => {}
);

Deno.test(
  "return a status object with a success key that is false when an error occurs during the Deno.run() process",
  () => {}
);

Deno.test(
  "return a status object with a success key that is true when the Deno.run() process finishes without error",
  () => {}
);

Deno.test("close all Deno processes that were opened during its run", () => {});
